El proyecto está completamente dockerizado para facilitar su ejecución e incluye la inicialización automática de la base de datos.

### Requisitos

- Docker y Docker Compose instalados.

### Pasos para ejecutar

1. Abra una terminal en la carpeta raíz del backend.
2. Ejecute el siguiente comando:

   ```bash
   docker-compose up --build
   ```
